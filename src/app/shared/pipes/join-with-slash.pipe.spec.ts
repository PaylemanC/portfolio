import { JoinWithSlashPipe } from './join-with-slash.pipe';

describe('JoinWithSlashPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinWithSlashPipe();
    expect(pipe).toBeTruthy();
  });
});
